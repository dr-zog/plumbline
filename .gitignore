# Local build output
/plumbline
/dist/
*.out
*.test
coverage.*

# Built engine binaries + checksums — shipped via releases, never committed (ADR 001/006)
/plugins/plumbline/bin/plumbline-*
/plugins/plumbline/bin/SHA256SUMS

# Machine-specific harness / editor config
/.mcp.json
.claude/settings.json
.claude/settings.local.json

# semantic-release tooling — installed on the fly in CI (ADR 005), never committed
/node_modules/
/package-lock.json
