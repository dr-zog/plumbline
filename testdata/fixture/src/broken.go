package src

// rotateKeys was meant to cover a component, but its anchor points at an ID that
// isn't in the register. The engine must flag this as a broken anchor.
//
// [impl->component~does-not-exist~1]
func rotateKeys() {}
